<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Traveler</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tag %>
</head>
<body>
    <div class="header">
      <div class="menu">
        <%= link_to 'Traveler', root_path %>

        <ul class="menu-list">
          <% if signed_in? -%>
            <li><%= link_to_if(current_user.is_admin?, 'User List', users_path){} %></li>
            <li><%= link_to 'Edit profile', edit_user_path(current_user.id) %></li>
            <li><%= link_to t('.sign_out'), sign_out_path, method: :delete %></li>
          <% else -%>
            <li><%= link_to t('.sign_in'), sign_in_path %></li>
            <li><%= link_to t('.sign_up'), sign_up_path %></li>
          <% end -%>
        </ul>
      </div>
    </div>

    <div class="content-wrapper">
      <div id="flash">
        <% flash.each do |key, value| -%>
          <div id="flash_<%= key %>"><%=h value %></div>
        <% end %>
      </div>

      <%= yield %>
    </div>

  </div>
</body>
</html>
